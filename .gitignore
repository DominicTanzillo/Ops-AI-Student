# IDE
.vscode/
.idea/
*.swp
*.swo

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.env
venv/

# GCP credentials (NEVER commit)
key.json
*.json.secret
credentials.json

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Local AI session notes (not for submission)
AI_USAGE_NOTES.md

# Office temp/lock files
~$*.docx
~$*.xlsx
~$*.pptx

# Week 4 - large data files (workflows sparse-checkout from upstream at run time)
week4/data/*.parquet

# Week 4 - answer-key artifacts (upstream removed these; we use them as
# self-check during development only, not for submission)
week4/data/monitoring_baseline.json
week4/data/week4_drift_manifest.json

# Week 4 - local analysis outputs (regenerated by scripts)
week4/data/level2_metrics.json

# Weeks 5-7 - large data files (sparse-checkout from upstream at run time)
week5/data/techcorp.db
week6/data/*.db
week7/data/*.db

# Weeks 5-7 - regenerated analysis outputs
week7/data/cost_breakdown_*.json
week7/data/eval_results_*.json
